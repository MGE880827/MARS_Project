# ##########################################################################################
# 專案名稱: 多重目標自動辨識系統 - XXX領域階層策略配置 (XXX Domain Stage Configuration)
# 維護日期: 2026-09-15
# 檔案路徑: MARS_Project/config/maps/stage_maps/rules/XXX_demo.py
# ##########################################################################################

"""
[名稱] 通用領域階層策略字典 (XXX Domain Stage Dictionary)
[作用] 定義通用領域 (XXX) 之策略字典：
       - TRAIN: 定義單階層訓練策略，跳過二階裁切投影，直接提供全圖保底模型之訓練參數與標籤規格。
       - INFER: [NOTE] 待開發
"""

# ==========================================================================================
# < TRAIN 字典配置說明表 >
# 1. stage_qty    : [int]  模型階層數量 (1: 單階層全圖訓練, 2: 二階層串聯裁切投影與微觀訓練)
# 2. stage_desc   : [str]  該領域訓練架構任務描述
# 3. stage_1      : [dict] 第一階層 (主領域) 訓練規格配置
#    - task_name  : [str]  任務名稱識別碼
#    - domain     : [list] 目標識別代碼清單 (e.g., ["VEH"])
#    - bbox_type  : [str]  邊界框類型 ("HBB": 水平框, "OBB": 旋轉框)
#    - gt_cls_ids : [list] 全景標註檔中代表主階層之物件類別編號清單
#    - cls_names  : [list] 物件類別名稱清單
#    - imgsz      : [int]  訓練輸入影像尺度 (寬高像素)
#    - bone_name  : [str]  基礎預訓練骨幹權重 (e.g., yolo11n.pt)
# 4. transition   : [dict] 單階層架構無銜接過渡參數，填入空字典 {}
# 5. stage_2      : [dict] 單階層架構無次階段訓練規格，填入空字典 {}
# ==========================================================================================

TRAIN = {
    # =============================================
    # ｜TRAIN｜XXX-XXX領域 (單階層架構)
    # =============================================
    "stage_qty"  : 1,
    "stage_desc" : "[MAIN] XXX-XXX (單階層模型訓練架構)",
    "stage_1"    : {
        "task_name"  : "XXX_DETECTION",
        "domain"     : ["XXX"],
        "bbox_type"  : "HBB",
        "gt_cls_ids" : [0],
        "cls_names"  : ["object"],
        "imgsz"      : 640,
        "bone_name"  : "yolo11n.pt"
    },
    "transition" : {},
    "stage_2"    : {}
}

# ==========================================================================================
# < INFER 字典配置說明表 > [NOTE] 待開發

# ==========================================================================================

INFER = {
    # =============================================
    # ｜INFER｜XXX-XXX領域 (單階層架構)
    # =============================================
    "stage_qty"  : 1,
    "stage_desc" : "[MAIN] XXX-XXX (單階層模型辨識架構)",
}